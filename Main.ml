(* Use Simulate to show all possible matches in most to least preferred order *)

Simulate.test ();
Simulate.test2 ();

(* Use SimStep to show all possible matches in most to least preferred order *)

SimStep.test ();
